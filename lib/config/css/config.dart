import 'package:flutter/material.dart';

class AppConfig {
  // Couleurs
  static const Color whiteColor = Color(0xFFFAF1E4); // Correction de la faute de frappe ici
  static const Color firstColor = Color(0xFFCEDEBD);
  static const Color secondColor = Color(0xFF9EB384); // Correction de la faute de frappe ici
  static const Color primaryColor = Color(0xFF435334);

  // Police
  static const String fontFamily = "Gaegu";
}
